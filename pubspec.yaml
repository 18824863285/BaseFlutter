name: wan_android_flutter
description: A new Flutter project.

publish_to:

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'


dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  analyzer: ^1.4.0 # null safe
  injectable: ^1.2.2 # null safe
  dio: ^4.0.0 # null safe
  provider: ^5.0.0 # null safe
  get_it: ^6.0.0 # null safe
  retrofit: any
  logger: any
  toast: ^0.1.5
  event_bus: ^2.0.0 # null safe
  json_annotation: ^4.0.1 # null safe
  json_serializable: ^4.1.0 # null safe
  extended_image: ^4.0.1 # null safe
  webview_flutter: 2.0.4 # null safe
  shared_preferences: ^2.0.5 # null safe
  flutter_localizations:
    sdk: flutter
  fluttertoast: ^8.0.3 # null safe
  pull_to_refresh: 2.0.0
  floor: 1.0.1 # null safe
  flutter_swiper_null_safety: 1.0.2
  keframe: 2.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.4.0
  injectable_generator:
  retrofit_generator: any
  floor_generator: 1.0.1 # null safe

flutter:
  uses-material-design: true

  assets:
    - assets/images/

flutter_intl:
  enabled: true
